name: image_blur_detection
description: A pure Dart package to detect blur, brightness, and contrast issues in images with configurable thresholds. Perfect for card scanning, document scanning, and photo validation.
version: 1.0.0
homepage: https://github.com/nolann-dev/image_blur_detection
repository: https://github.com/nolann-dev/image_blur_detection
issue_tracker: https://github.com/nolann-dev/image_blur_detection/issues

topics:
  - image
  - blur-detection
  - image-processing
  - quality-validation
  - flutter

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  image: ^4.3.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
