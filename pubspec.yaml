name: image_blur_detection
description: A Flutter/Dart package to detect blur, brightness, and contrast issues in images with configurable thresholds.
version: 1.0.0
homepage: https://github.com/yourusername/image_blur_detection
repository: https://github.com/yourusername/image_blur_detection

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  image: ^4.3.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
